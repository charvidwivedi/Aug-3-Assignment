/** @format */

import { AppRegistry } from 'react-native';
import App from './App';
import Comb from './Comb';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Comb);
